namespace OpenClaw.Node
{
    internal static class BuildInfo
    {
        // Increment this on every code change that should be easy to verify at runtime.
        public const string BuildVersion = "b0031";
    }
}
